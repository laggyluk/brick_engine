#version 330 corelayout (location = 0) in vec4 position;layout (location = 1) in vec4 color;out VertexData {	vec4 worldPos;    vec3 colour;	vec3 normal;} oData;uniform mat4 gWorld;uniform mat4 mvp;void main(){	oData.colour = color.xyz;	int i = int(color[3]);	float x = (i / 100)-2;	float y = ((i / 10) % 10)-2;    float z = (i % 10)-2;  		oData.normal = vec3(0,0,0);	oData.worldPos = gWorld * position;	gl_Position = mvp * position;}